project:
  name: evidence_hierarchical_damage_classifier
  output_dir: outputs/fast_debug

dataset:
  debug_subset: 4
  debug_max_samples: 8

training:
  epochs: 1
  batch_size: 2
  num_workers: 0

eval:
  batch_size: 2
  num_workers: 0
